" .vimrc in your repo root
set tabstop=2       " Number of spaces a <Tab> counts for
set shiftwidth=2    " Number of spaces used for autoindent
set expandtab       " Use spaces instead of literal tab characters

